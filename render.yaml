services:
  - type: web
    name: naija-tax-guide
    env: node
    buildCommand: npm install && pip3 install -r server/python/requirements.txt && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PYTHON_VERSION
        value: 3.11.9
